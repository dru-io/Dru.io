/**
 * @file
 * Used for testing REST resources.
 */
{
  "dev": {
    "host": "localhost",
    // This is hash for login and password.
    // base64_encode($username . ':' . $password)
    // Default value for root:root pair.
    "authorization": "Basic cm9vdDpyb290"
  },

  "prod": {
    "host": "dru.io"
  }
}
