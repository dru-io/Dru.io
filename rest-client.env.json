/**
 * @file
 * Used for testing REST resources.
 *
 * @see https://www.jetbrains.com/help/phpstorm/rest-client-in-phpstorm-code-editor.html
 */
{
  "dev": {
    "__comment": "This is hash for login and password. base64_encode($username . ':' . $password). Default value for root:root pair.",
    "host": "druio8.localhost",
    "authorization": "Basic cm9vdDpyb290"
  },

  "prod": {
    "host": "dru.io"
  }
}
